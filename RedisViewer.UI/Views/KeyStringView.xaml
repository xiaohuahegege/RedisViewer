<UserControl x:Class="RedisViewer.UI.Views.KeyStringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:icons="http://schemas.imiyu.cc/icons"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:uc="clr-namespace:RedisViewer.UI.Views.Components"
             x:ClassModifier="internal">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/KeyViewerViewStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <uc:ValueEditor Field="Value" Value="{Binding Value, Mode=TwoWay}" Grid.Row="0" />
        <Button Style="{StaticResource ButtonStyle}" Grid.Row="1" Height="28" Width="80" HorizontalAlignment="Right" Margin="0,20,0,0" Command="{Binding SaveCommand}">
            <StackPanel Orientation="Horizontal" Margin="6,0">
                <icons:FIcon Icon="Save"  Size="13" Foreground="#606060" VerticalAlignment="Center" />
                <TextBlock Text="Save" Style="{StaticResource ButtonTextStyle}" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>